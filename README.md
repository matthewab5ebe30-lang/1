# Telegram bot (aiogram + PostgreSQL)

Минимальный рабочий бот на Python 3.11+, `aiogram v3`, `asyncpg`.

## Что умеет
- Каталог квартир с фильтрами и пагинацией.
- Экран бронирования (переход только на сайт): https://homereserve.ru/bJOig2XsQu
- Карусель медиа (фото/видео) в карточке квартиры: переключение ◀️/▶️ и открытие текущего файла.
- Промокоды (welcome/manual/giveaway/referral_reward), реферальная ссылка, кабинет.
- Розыгрыш с выбором победителя, выдачей кода и анонсом в канал.
- Заявки на даты с уведомлением админов и обработкой через inline-кнопки.
- Админ-панель (только кнопками), статистика, рассылка, управление квартирами и промокодами.
- Фоновый цикл напоминаний неактивным пользователям.

## Структура проекта (для деплоя)
- `app.py` — весь код бота: хендлеры, FSM, SQL-схема, создание таблиц, фоновые задачи.
- `requirements.txt` — зависимости Python.
- `.env.example` — пример переменных окружения.
- `Procfile` — команда процесса для платформ с worker-моделью (`worker: python app.py`).
- `runtime.txt` — фиксированная версия Python для хостинга.
- `.gitignore` — исключения (`.env`, `__pycache__`, виртуальные окружения).
- `README.md` — инструкция запуска и сценарии использования.

## Переменные окружения
Обязательные:
- `BOT_TOKEN`
- `DATABASE_URL`

Рекомендуемые:
- `ADMIN_IDS` (например `123,456`)
- `ADMIN_CHAT_ID`
- `CHANNEL_ID`
- `BOOKING_URL`
- `MANAGER_PHONE` (для кнопки `📞 Позвонить`)
- `MANAGER_TG_USERNAME` (для кнопки `✔️ Написать в ТГ`)
- `MANAGER_WHATSAPP` (для кнопки `🟢 Написать в WhatsApp`)
- `BUDGET_THRESHOLDS` (например `3000,5000`)
- `STRICT_PHONE_MODE` (`true/false`)

## Быстрый запуск в Codespaces
1. Скопируйте пример: `cp .env.example .env`
2. Заполните `.env` своими значениями.
3. Установите зависимости: `pip install -r requirements.txt`
4. Проверка синтаксиса: `python -m py_compile app.py`
5. Запуск: `python app.py`

## Деплой (общий сценарий)
1. Загрузите проект в репозиторий.
2. На хостинге добавьте env-переменные (из `.env`, но без коммита секретов).
3. Убедитесь, что стартовая команда берётся из `Procfile` (`python app.py`).
4. Запустите деплой.
5. Проверьте в логах строку `Start polling`.

## Команды пользователя
- `/start`
- `/help`
- `/privacy`
- `/delete_me`

## Подробный путь обычного пользователя
1. Пользователь нажимает `/start` и видит главное меню.
2. Нажимает `🏠 Квартиры` и попадает в каталог с пагинацией.
3. При необходимости открывает `⚙️ Фильтры`:
	- выбирает гостей,
	- выбирает бюджет,
	- отмечает теги,
	- нажимает `✅ Применить`.
4. Выбирает квартиру `🏙 ... • от ... ₽` и открывает карточку.
5. В карточке доступны действия:
	- `📸 Фото и описание` (переход в Telegram-пост),
	- `📍 На карте`,
	- `📅 Забронировать` (переход на сайт),
	- `✍️ Указать даты`.
6. Если нажата `✍️ Указать даты`, пользователь отправляет одним сообщением даты и гостей.
7. Бот подтверждает, что заявка передана администратору.
8. В разделе `🎁 Промокод` пользователь может:
	- получить welcome-код (если доступен),
	- взять реферальную ссылку,
	- посмотреть свои коды.
9. В разделе `🏆 Розыгрыш` пользователь нажимает `✅ Участвовать`.
10. В разделе `👤 Кабинет` видит:
	- свою реф-ссылку,
	- количество приглашённых,
	- количество квалифицированных,
	- число выданных ему промокодов.

## Подробный путь администратора
1. Админ открывает `/start` и нажимает `🛠 Админ-панель`.
2. `🏠 Квартиры`:
	- добавление квартиры через мастер (пошагово + предпросмотр),
- редактирование уже добавленной квартиры через мастер,
	- предпросмотр поста и публикация поста в канал кнопкой,
	- просмотр списка,
	- включение/выключение,
	- редактирование цены, URL, тегов, порядка.
3. `🎁 Промокоды`:
	- добавить один код,
	- bulk-загрузка кодов,
	- просмотр пулов и остатка.
4. `📥 Заявки`:
	- открыть заявку,
	- `✅ Ответить` пользователю,
	- `🎁 Выдать промокод`,
	- `🟡 Пометить обработано`.
5. `🏆 Розыгрыш`:
	- посмотреть число участников,
	- запустить `🎲 Разыграть`.
6. `📊 Статистика`:
	- метрики за 7/30 дней,
	- топ квартир.
7. `📣 Рассылка`:
	- выбрать сегмент,
	- отправить текст,
	- подтвердить рассылку.

## Понятные форматы ввода (админ)

### 1) Добавить квартиру
Теперь используется мастер с шагами:
- блок «Основа» (название, адрес в шапке, формат, площадь, этаж, размещение),
- блок «Описание» (краткое описание локации, что рядом, что есть в квартире),
- блок «Публикация» (короткий адрес, цена, теги, медиа, ссылки, сортировка).

Для уже существующей квартиры:
- откройте `🏠 Квартиры` → `📋 Список` → нужную квартиру,
- нажмите `🧩 Мастер редактирования`,
- пройдите шаги и сохраните предпросмотр.

Перед стартом мастера:
- Подготовьте основные данные по квартире (формат, площадь, этаж, описание).
- На шаге медиа отправьте фото/видео прямо файлами в чат с ботом.
- Подготовьте ссылку на карту (`map_url`) и, при необходимости, ссылку на пост канала (`channel_post_url`).

В конце мастер показывает красивый предпросмотр и кнопки:
- `✅ Сохранить`
- `🔁 Заполнить заново`
- `❌ Отменить`

После сохранения/редактирования квартиры:
- в карточке админа есть `📣 Предпросмотр поста` и `📤 В канал`.
- бот формирует пост с кнопками:
	- `🗺 Карта`
	- `📅 Бронь`
	- `👤 Менеджер`

### 2) Добавить один промокод
Формат:
`КОД|ТИП`

Типы:
- `welcome`
- `giveaway`
- `manual`
- `referral_reward`

Примеры:
- `WELCOME100|welcome`
- `SPRING15|manual`

### 3) Bulk-загрузка промокодов
В одном сообщении:
- 1-я строка: тип
- Далее: коды, каждый с новой строки

Пример:
```text
manual
APRIL-100
APRIL-200
APRIL-300
```

### 4) Ответ по заявке
Отправьте обычный текст одним сообщением.

Пример:
`Добрый день! Свободно с 20.03 по 23.03. Для подтверждения нажмите «Забронировать» в карточке квартиры.`

### 5) Ручная выдача промокода по заявке
Просто отправьте код одним сообщением.

Пример:
`SPRING-15`

## Заметки
- Таблицы PostgreSQL создаются автоматически при старте.
- Миграции (Alembic) не используются.
- Все админ-действия выполняются через inline-кнопки, без `/admin` команд.
- В интерфейсе бота добавлена подпись разработчика: `@andreuanderson`.